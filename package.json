{
  "name": "rajiv-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Rajiv Bhardwaj — Full Stack Developer & AI Enthusiast portfolio (plain Node.js + HTML/CSS/JS)",
  "main": "server.js",
  "scripts": {
    "css:build": "tailwindcss -i ./src/input.css -o ./public/styles.css --minify",
    "css:watch": "tailwindcss -i ./src/input.css -o ./public/styles.css --watch",
    "dev": "npm run css:build && concurrently -n css,server -c yellow,cyan \"npm run css:watch\" \"node server.js\"",
    "build": "npm run css:build",
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "concurrently": "8.2.2",
    "tailwindcss": "3.4.6"
  }
}
